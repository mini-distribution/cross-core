diff -Naur a/src/shared/log.c b/src/shared/log.c
--- a/src/shared/log.c	2012-09-21 12:07:41.885429447 +0200
+++ b/src/shared/log.c	2012-09-24 21:17:34.336884615 +0200
@@ -705,8 +705,11 @@
                 va_start(ap, format);
                 while (format && n + 1 < ELEMENTSOF(iovec)) {
                         char *buf;
+                        va_list ap1, ap2;
+                        va_copy(ap1, ap);
+                        va_copy(ap2, ap);
 
-                        if (vasprintf(&buf, format, ap) < 0) {
+                        if (vasprintf(&buf, format, ap1) < 0) {
                                 r = -ENOMEM;
                                 goto finish;
                         }
@@ -717,7 +720,9 @@
                         iovec[n].iov_len = 1;
                         n++;
 
-                        format = va_arg(ap, char *);
+                        format = va_arg(ap2, char *);
+                        va_end(ap1);
+                        va_end(ap2);
                 }
 
                 zero(mh);
@@ -737,21 +742,30 @@
         } else {
                 char buf[LINE_MAX];
                 bool found = false;
+                va_list aq;
 
                 /* Fallback if journal logging is not available */
 
                 va_start(ap, format);
                 while (format) {
-
-                        vsnprintf(buf, sizeof(buf), format, ap);
+                        
+                        va_copy(aq, ap);
+                        
+                        log_info("calling vsnprintf()");
+                        vsnprintf(&buf, sizeof(buf), format, aq);
+                        va_end(aq);
                         char_array_0(buf);
+                        log_info("format=%p", format);
 
                         if (startswith(buf, "MESSAGE=")) {
                                 found = true;
                                 break;
                         }
 
-                        format = va_arg(ap, char *);
+                        va_copy(aq, ap);
+                        format = va_arg(aq, char *);
+                        va_end(aq);
+                        log_info("format=%p", format);
                 }
                 va_end(ap);
 
